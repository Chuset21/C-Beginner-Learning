int main() {
    const char *pChar;      // Value cannot be changed through this pointer
    long *const pLong;      // Pointer address cannot be changed

    const int *const pInt;  // Neither can be changed

    return 0;
}